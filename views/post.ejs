<h1><%= post.title %></h1>

<p><%= post.content %></p>

<h3>Comments</h3>
<ul>
  <% (post.comments || []).forEach(comment => { %>
    <li><%= comment.content %> — <i><%= comment.author %></i></li>
  <% }) %>
</ul>
